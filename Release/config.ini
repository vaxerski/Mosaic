[Mosaic]
max_clones = 50000         ;max amount of images on screen. Increase if mosaic doesnt produce full image.
precision_point = 0.01     ;smaller precision point = more detail, more clones. DEFAULT 0.01