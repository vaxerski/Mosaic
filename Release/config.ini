[Mosaic]
max_clones = 5000000         ;max amount of images on screen. Increase if mosaic doesnt produce full image.
precision_point = 0.003    ;smaller precision point = more detail, more clones. DEFAULT 0.01
debug_info = false